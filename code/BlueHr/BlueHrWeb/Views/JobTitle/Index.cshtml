@using MvcPaging

@model IPagedList<BlueHrLib.Data.JobTitle>

@{
    ViewBag.Title = "职位管理";
}

@*@Styles.Render("~/Content/degreetype")*@

<ol class="breadcrumb current-position" style="margin-top:10px;">
    您的位置：
    <li>基础信息</li>
    <li>职位管理</li>
</ol>

<hr class="hr-grey" />

<div class="row" style="margin:0;">
    <form action="/JobTitle/Search" method="get">
        <div class="col-lg-4 col-md-4 col-sm-6 col-xs-12">
            <div class="marco-igroup-primary">
                <span>职位名称</span>
                <input type="text" name="Name" id="Name" placeholder="职位名称" value="@ViewBag.Query.Name" />
            </div>
        </div>
        <div class="col-lg-4 col-md-4 col-sm-6 col-xs-6" style="margin-top:5px;">
            <button type="submit" class="marco-btn-success">
                <i class="glyphicon glyphicon-search"></i>
                搜索
            </button>
        </div>
    </form>
</div>

<div style="margin:10px;">
    <div class="minitor-show marco-tabs-default">
        <button type="button" class="marco-btn-primary import-dialog-btn pull-right" style="margin-top: -5px; margin-right:15px;">
            <i class="fa fa-cloud-upload"></i>
            <span>上传</span>
        </button>

        <a href="/Staff/Create" class="pull-right" style="margin-top: -10px; margin-right:5px;">
            <button class="marco-btn-primary">
                <i class="glyphicon glyphicon-plus-sign"></i>
                <span>创建</span>
            </button>
        </a>

        <ul class="nav nav-tabs" role="tablist" style="width: 400px;">
            <li role="presentation" class="active"><a href="#on_job" role="tab" data-toggle="tab">当前职位</a></li>
            <li role="presentation"><a href="#off_job" role="tab" data-toggle="tab">历史职位</a></li>
            <li role="presentation"><a href="#all_job" role="tab" data-toggle="tab">全部职位</a></li>
        </ul>

        <div class="tab-content">

            <div role="tabpanel" class="tab-pane active" id="on_job">
                <div calss="col-lg-12 col-md-12 col-sm-12 col-xs-12" id="on_job_grid_table" style="margin:auto;"></div>
                <table class="table table-bordered" style="display:none;">
                    <thead>
                        <tr>
                            <th>职位名称</th>
                            <th>必须证照</th>
                            <th>备注</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                if (!item.IsRevoked.Value)
                                {
                                <tr>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.name)
                                    </td>
                                    <td>
                                        <span>@item.CertificateTypeStr</span>
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.remark)
                                    </td>
                                    <td>
                                        <a href="/JobTitle/Delete/@item.id" target="_blank" class="option-icon-primary" title="详情">
                                            <i class="fa fa-eye"></i>
                                        </a>

                                        <a href="/JobTitle/Edit/@item.id" target="_blank" class="option-icon-warning" title="编辑">
                                            <i class="fa fa-pencil"></i>
                                        </a>
                                    </td>
                                </tr>
                                }
                            }
                        }

                    </tbody>
                </table>
            </div>



            <div role="tabpanel" class="tab-pane" id="off_job">
                <div calss="col-lg-12 col-md-12 col-sm-12 col-xs-12" id="off_job_grid_table" style="margin:auto;"></div>
                <table class="table table-bordered" style="display:none;">
                    <thead>
                        <tr>
                            <th>职位名称</th>
                            <th>必须证照</th>
                            <th>备注</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                if (item.IsRevoked.Value)
                                {

                                <tr>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.name)
                                    </td>
                                    <td>
                                        <span>@item.CertificateTypeStr</span>
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.remark)
                                    </td>
                                    <td>
                                        <a href="/JobTitle/Delete/@item.id" target="_blank" class="option-icon-primary" title="详情">
                                            <i class="fa fa-eye"></i>
                                        </a>

                                        <a href="/JobTitle/Edit/@item.id" target="_blank" class="option-icon-warning" title="编辑">
                                            <i class="fa fa-pencil"></i>
                                        </a>
                                    </td>
                                </tr>
                                }
                            }
                        }

                    </tbody>
                </table>
            </div>


            <div role="tabpanel" class="tab-pane" id="all_job">
                <div calss="col-lg-12 col-md-12 col-sm-12 col-xs-12" id="all_job_grid_table" style="margin:auto;"></div>
                <table class="table table-bordered" style="display:none;">
                    <thead>
                        <tr>
                            <th>职位名称</th>
                            <th>必须证照</th>
                            <th>备注</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                <tr>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.name)
                                    </td>
                                    <td>
                                        <span>@item.CertificateTypeStr</span>
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.remark)
                                    </td>
                                    <td>
                                        <a href="/JobTitle/Delete/@item.id" target="_blank" class="option-icon-primary" title="详情">
                                            <i class="fa fa-eye"></i>
                                        </a>

                                        <a href="/JobTitle/Edit/@item.id" target="_blank" class="option-icon-warning" title="编辑">
                                            <i class="fa fa-pencil"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>

        </div>
    </div>
</div>



@*@Scripts.Render("~/bundles/degreetype")*@

<script type="text/javascript">
    Layout.init();

    $(window).resize(function () {
        Layout.TransferTableToGrid($(window).width() - 300, $(window).height() - 250, false, "on_job", "当前职位列表", "20", "on_job_grid_table");
        Layout.TransferTableToGrid($(window).width() - 300, $(window).height() - 250, false, "off_job", "历史职位列表", "20", "off_job_grid_table");
        Layout.TransferTableToGrid($(window).width() - 300, $(window).height() - 250, false, "all_job", "所有职位列表", "20", "all_job_grid_table");
    })

    Layout.TransferTableToGrid($(window).width() - 300, $(window).height() - 250, false, "on_job", "当前职位列表", "20", "on_job_grid_table");


    $('#off_job').addClass("active");
    Layout.TransferTableToGrid($(window).width() - 300, $(window).height() - 250, true, "off_job", "历史职位列表", "20", "off_job_grid_table");
    $('#off_job').removeClass("active");

    $('#all_job').addClass("active");
    Layout.TransferTableToGrid($(window).width() - 300, $(window).height() - 250, false, "all_job", "所有职位列表", "20", "all_job_grid_table");
    //Layout.TransferTableToGrid($(window).width() - 300, $(window).height() - 250, false, "all_job", "历史职位列表", "20", "all_job_grid_table");
    $('#all_job').removeClass("active");

</script>
